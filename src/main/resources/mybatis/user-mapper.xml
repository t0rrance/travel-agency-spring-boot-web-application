<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travelagency.mapper.UserMapper">

    <insert id="insertUser">
        insert into users (
        user_name
        ,first_name
        ,last_name
        ,password
        ) values (
        #{user.userName}
        ,#{user.firstName}
        ,#{user.lastName}
        ,#{user.password}
        )
    </insert>

    <select id="selectUserByUserName" resultType="com.example.travelagency.controller.model.user.UserResponse">
        select user_name
          ,first_name
          ,last_name
        from  users
    </select>

</mapper>